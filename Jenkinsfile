pipeline {
    agent any

    stages {
        stage("AWS Demo") {
            steps {
                 
                 step("step1") {
                 echo "I am Stage One Step"                    
               }

               step("step 2") { 
                echo " I am in step 2"

            } 
        }
    }
 }
}
