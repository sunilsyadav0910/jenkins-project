pipeline {
    agent any

    stages {
        stage("AWS Demo") {
            steps {
                 echo "I am Stage One Step"                    
                echo " I am in step 2"
                  } 
        }
    }
 }
}
