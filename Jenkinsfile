pipeline {
    agent any

    stages {
        stage("AWS Demo") {
            steps {
                 echo "I am Stage One Step"                    
                 echo " I am in step 2"
                  } 
        }  
        
       stage("stage 2") { 
             steps { 
              echo "I am in stage 2"
              echo env
           } 
         }             


    }
 }

